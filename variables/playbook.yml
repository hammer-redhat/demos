---
- name: Test vars
  hosts: all 
  become: yes
  vars_files:
    - variables.yml

  tasks:
    - name: Install required packages.
      yum:
        name: "{{ item }}"
        state: present
      loop: packages