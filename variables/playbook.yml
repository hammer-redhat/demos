---
- name: Test vars
  hosts: all 
  become: yes
  vars_files:
    - variables.yml

  tasks:
    - name: Install required packages.
      yum:
        name: "{{ item.name }}"
        state: "{{ item.state }}"
      loop:
        - "{{ packages }}"